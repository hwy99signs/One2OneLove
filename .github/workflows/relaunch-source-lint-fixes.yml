name: Relaunch Exact Source Lint Fixes (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: One-time repair completed
        run: echo "The one-time relaunch source lint repair has already been applied."
